---
import Card from '../atoms/Card.astro';
import Input from '../atoms/Input.astro';
import Button from '../atoms/Button.astro';
---

<Card>
  <h2 class="text-xl font-semibold text-gray-900 mb-6">Enviar PQRS</h2>
  
  <form id="pqrs-form" class="space-y-6">
    <div>
      <label class="block text-sm font-medium text-gray-700 mb-2">
        Tipo de Solicitud
      </label>
      <select 
        name="id_tipo" 
        required
        class="block w-full rounded-md border-gray-300 shadow-sm focus:border-primary-500 focus:ring-primary-500 sm:text-sm"
      >
        <option value="">Selecciona el tipo</option>
        <option value="1">Petición</option>
        <option value="2">Queja</option>
        <option value="3">Reclamo</option>
        <option value="4">Sugerencia</option>
      </select>
    </div>
    
    <Input
      type="text"
      name="asunto"
      label="Asunto"
      placeholder="Describe brevemente el tema"
      required
    />
    
    <Input
      type="textarea"
      name="mensaje"
      label="Mensaje"
      placeholder="Describe detalladamente tu solicitud..."
      rows={6}
      required
    />
    
    <div class="bg-blue-50 border border-blue-200 rounded-md p-4">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
          </svg>
        </div>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-blue-800">
            Información importante
          </h3>
          <div class="mt-2 text-sm text-blue-700">
            <p>Te responderemos en un plazo máximo de 15 días hábiles. Mantén tu número de referencia para hacer seguimiento.</p>
          </div>
        </div>
      </div>
    </div>
    
    <Button type="submit" class="w-full">
      Enviar PQRS
    </Button>
  </form>
</Card>

<script>
  document.getElementById('pqrs-form')?.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const formData = new FormData(e.target as HTMLFormElement);
    const pqrsData = Object.fromEntries(formData.entries());
    
    try {
      // TODO: Enviar PQRS a Supabase
      console.log('Enviando PQRS:', pqrsData);
      
      // Simulación de éxito
      const referenceNumber = Math.floor(Math.random() * 1000000);
      alert(`¡PQRS enviado exitosamente! Tu número de referencia es: ${referenceNumber}`);
      (e.target as HTMLFormElement).reset();
      
    } catch (error) {
      console.error('Error al enviar PQRS:', error);
      alert('Hubo un error al enviar tu PQRS. Inténtalo de nuevo.');
    }
  });
</script>